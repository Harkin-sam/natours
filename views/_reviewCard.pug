//- mixin syntax, mixin is just a function 
mixin reviewCard(review)
    .reviews__card
        .reviews__avatar
            img.reviews__avatar-img(src=`/img/users/${review.user[0].photo}`, alt=`${review.user[0].name}`)
            h6.reviews__user= review.user[0].name
        p.reviews__text= review.review
        .reviews__rating
            each star in [1, 2, 3, 4, 5]
                //- rating classes manipulation
                svg.reviews__star(class=`reviews__star--${review.rating >= star ? 'active': 'inactive' }`)
                    use(xlink:href='/img/icons.svg#icon-star')

            //-  we use the (class= '') for css class modifier that changes based on data condition